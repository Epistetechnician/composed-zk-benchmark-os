# Plasticity Recovery V1 Execution Authorization

State slice: `continual-learning-plasticity-recovery-v1`.

## Authorization status

On 2026-08-29, the user authorized the new continual-learning slice with
replay, selective low-utility reinitialization, untouched-base, fixed-adapter,
replay, reinitialization, and combined arms. This opens the exact synthetic
contract and its local validation artifacts only.

## Allowed

- fresh deterministic synthetic fit/tune/assessment panels;
- the five fixed arms in the protocol;
- equal-budget reversible adapter analogues;
- sealed prediction locking before assessment effects;
- unchanged base state;
- external aggregate artifact output;
- independent readback validation and documentation.

## Not allowed

This authorization does not allow base-weight updates, adapter merges, model
or corpus downloads, model-bearing execution, Astral execution, Astral ledger
mutation, V48 or Phase 832 scientific-artifact reuse, introspection claims,
benchmark claims, production traffic, or ZK/PQC evidence.

GiveMeANode execution remains pending a user-specified hard USD spend ceiling.
No paid remote job is submitted under this authorization. If later opened, it
must execute the sealed artifact with an immutable command and report provider
identity, runtime, cost, artifact digest, and independent validation.

Every mutation in this authorization record touches state slice
`continual-learning-plasticity-recovery-v1`.
