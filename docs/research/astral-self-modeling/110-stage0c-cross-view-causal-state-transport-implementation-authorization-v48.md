# Astral V48 implementation authorization — cross-view causal state transport

State slice: `astral-stage0c-cross-view-causal-state-transport-v48`.

Date: 2026-08-28.

Status: `ImplementationAuthorized / QualificationOnlyInitially / AssessmentClosed`.

## Authorization basis

This implementation authorization follows the accepted independent review
receipt [V48 review round 2](109-v48-independent-design-review-r2-2026-08-28.md).
It authorizes the V48 implementation and qualification boundary only. It does
not authorize assessment effects until the qualification, fit/tune lock, power
simulation, configuration seal, and independent pre-assessment review gates
pass.

The rejected V47 slice, V46, V39-V45 scientific artifacts, V25, V28-V29,
V61, and V82 are not V48 inputs. V82 remains an isolated Neural Chameleon
branch and remains blocked for missing artifacts.

## Exact actor and runtime custody

| Item | Sealed value |
|---|---|
| actor | `Qwen3.6-35B-A3B-MLX-4bit` |
| model root | `/Users/shaanp/.lmstudio/models/lmstudio-community/Qwen3.6-35B-A3B-MLX-4bit` |
| architecture | `Qwen3_5MoeForConditionalGeneration` |
| layer count | `40` |
| hidden width | `2048` |
| Python | `3.14.5` |
| MLX | `0.31.2` |
| MLX-LM | `0.31.3` |
| execution network | disabled during model execution |
| training | forbidden; no weight or adapter update |

The runner must re-custody the model directory and source/runtime manifests
before loading weights. A manifest or runtime digest mismatch is a terminal
qualification failure. The authorization does not copy or mutate model files.

## Exact access operator and target cells

The only scientific operator is one additional deep-to-shallow transport pass:

```text
source_layer       = 26
destination_layer  = 12
position           = final tokenized state-anchor position
additional_passes  = 1
alpha              = 0.10
normalization      = source vector rescaled to receiver L2 norm
P=0,N=0            = receiver carrier identity reinsertion
P=1,N=0            = state-matched donor replacement
P=0,N=1            = state-shuffled, norm-matched generic donor replacement
P=1,N=1            = forbidden and validator-rejected
```

The donor permutation for `N=1` is generated by a fixed label-independent
counterbalancing seed and is frozen in the panel manifest. The operator, layer,
position, alpha, normalization, pass count, state anchor, margin orientation,
and control construction cannot be tuned or changed after fit data are read.

Qualification separately checks native parity, deterministic repeatability,
identity/no-op replacement, finite zero replacement, nonzero intervention reach
to the response logits, capture/replacement shape `(batch, sequence, 2048)`,
40-layer traversal, and source/runtime/model custody. Any failure stops V48.

## Fresh corpus and panel identity

The fresh external corpus identity is:

```text
astral-v48-gutenberg-cross-view-state-transport-r1-2026-08-28
```

The corpus is a new 48-document Project Gutenberg context panel selected from
a fixed catalog snapshot by the sealed metadata-only selector in the V48
runner. The selector excludes every V39-V47 source and reserved identifier,
rejects duplicate/near-duplicate titles and authors, requires sufficient
normalized prose for the fixed context window, and assigns 16 documents each
to fit, tune, and assessment before any model execution. The source catalog,
selection manifest, downloaded bytes, license/public-domain basis, and complete
file census are digested in the external custody root.

Each document contributes four state-transition families, for 192 total
families. Every family has four-valued state identity, two independently seeded
semantic views, a receiver/donor pair, a fixed response mapping, and all
permitted transport/null/direction cells. Fit, tune, and assessment are
disjoint by document, author, exact/normalized text, codebook seed, generator
seed, template, state graph, edge, and counterfactual mapping.

Fresh corpus custody root:

```text
/Users/shaanp/Documents/astral-artifacts/astral-v48-gutenberg-cross-view-state-transport-r1-2026-08-28
```

This root must be repository-external and immutable after sealing. The V48
runner may acquire public Gutenberg bytes directly or receive a GiveMeANode
artifact bundle, but the selected bytes and generated panel must pass the same
independent digest, census, split, and duplicate validators. A GiveMeANode
receipt is a transport/custody input, not scientific evidence or protocol
authorization.

## Runner, validator, and retained result root

The additive implementation paths are fixed:

| Role | Path or identity |
|---|---|
| pure contract | `tools/astral-stage0c-qwen36-v48/protocol_v48.py` |
| fresh corpus acquisition | `tools/astral-stage0c-qwen36-v48/acquire_gutenberg_v48.py` |
| state panel builder | `tools/astral-stage0c-qwen36-v48/panel_v48.py` |
| qualification runner | `tools/astral-stage0c-qwen36-v48/qualify_v48.py` |
| scientific runner | `tools/astral-stage0c-qwen36-v48/run_causal_state_transport_v48.py` |
| independent validator | `tools/astral-stage0c-qwen36-v48/validate_v48.py` |
| qualification output | `/Users/shaanp/Documents/astral-artifacts/astral-v48-qualification-r1-2026-08-28` |
| panel output | `/Users/shaanp/Documents/astral-artifacts/astral-v48-panel-r1-2026-08-28` |
| measurement output | `/Users/shaanp/Documents/astral-artifacts/astral-v48-measurement-r1-2026-08-28` |
| immutable result root | `/Users/shaanp/Documents/astral-artifacts/astral-v48-results-r1-2026-08-28` |

The independent validator must be a separate process and must reject raw
prompts, activations, logits, generated text, per-token telemetry, PII,
credentials, undeclared fields, digest drift, split leakage, incomplete cells,
lock-order drift, or assessment output without review.

## Required execution order

1. Create the external custody root and acquire the fresh Gutenberg source
   bytes or a digest-bound GiveMeANode bundle.
2. Build and independently validate the 48-document/192-family panel.
3. Run qualification only. Do not run fit, tune, or assessment if any
   qualification gate fails.
4. Run the sealed power simulation and fit/tune measurement. Emit and digest
   tune predictions before generating tune effects.
5. Freeze the predictor, operator, feature map, controls, metric, thresholds,
   sample counts, missingness rule, and retention policy.
6. Obtain an independent review receipt against the sealed configuration.
7. Open assessment only after the receipt is valid; retain aggregate results
   only; run the independent validator.

No adaptive layer, wrapper, position, alpha, threshold, corpus, panel,
feature-map, control, estimand, or missingness changes are permitted. A failed
gate is terminal for this slice.

## Claim ceiling and permitted dispositions

The maximum claim ceiling is:

```text
LocalDevelopmentV48BoundedCausalStateTransportOnly
```

Permitted final classifications are `InstrumentFeasibility`,
`DevelopmentNoCandidate`, and `BoundedCausalStateTransportResult`. The last
classification is bounded to the named Qwen3.6 actor, exact operator, state
graph, task, corpus, and runtime. It does not establish introspection,
privileged self-access, causal self-modeling, consciousness, Stage 0C, Stage 1,
benchmark evidence, or production readiness. Stage 0C and Stage 1 remain
blocked unless their existing independent gates are separately satisfied.

## Authorization status

`AUTHORIZED_FOR_IMPLEMENTATION_AND_QUALIFICATION_ONLY`.

Assessment is `CLOSED`. Qualification, fit/tune, prediction locking, power
simulation, configuration sealing, independent pre-assessment review, and
aggregate-only validation are mandatory prerequisites. V47 remains rejected;
V46 remains permanently closed; V82 remains isolated and blocked.
