# V31R2 Corrected Execution Authorization

State slice: `astral-rgs-v31r2-tiny-acquisition-execution`.

Status: `AuthorizedOnce / NotRun`.

Bind RGS `aee23d54c04ecf145999331f58290303b143acc3` and Astral
`b5033ae3922f45803249445242047bd9c925b10a`. V31R2 changes only the
inventory identity binding proven by the regression test. Corpus, evaluator,
model, seed, optimizer, learning rate, adapter geometry, update budget, gates,
and claim ceiling remain frozen. A new ledger consumes the R2 identity.
