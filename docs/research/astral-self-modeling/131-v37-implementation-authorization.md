# V37 Implementation Authorization

Status: `ImplementationAuthorized / ExecutionNotAuthorized`.

Authorize additive implementation of the preregistered V37 fixture, offline
one-process MLX worker, exclusive coordinator, hermetic tests, source locks,
and independent model-free Astral validator.

Every task, arm, seed, order, schedule, token budget, evaluation point,
useful-difficulty gate, and claim ceiling in V37 remains frozen. The worker
must run tokenizer preflight over all possible training prompts before its
first gradient step.

Model execution, CL-bench, assessment, dynamic selection, threshold changes,
confirmation, SOTA, and higher claims remain unauthorized.
