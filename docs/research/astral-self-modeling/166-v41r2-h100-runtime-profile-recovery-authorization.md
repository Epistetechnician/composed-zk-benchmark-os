# V41R2 H100 Runtime Profile Recovery Authorization

State slice: `V41R2H100RuntimeProfileRecovery`.

Status: `ProspectivelyAuthorized / NotRun`.

V41R2 is a fresh infrastructure-recovery identity, not reuse of failed V41 job
`job-psr8z`. It binds the exact V41 RGS source
`0403e731a91ead32f895b3822db8bcd044424f13`, unchanged GPT-OSS-20B revision,
corpus, one-step LoRA profile, rollback check, result schema, and independent
validator.

The only orchestration amendment is `max_restarts=1`. A provider-managed
restart may recover from worker loss. The run remains limited to one
clock-locked H100, 300 minutes, and `$13.50`. Tune, assessment, pilot,
qualification, Astral selection, and claim promotion stay forbidden.

A completed experiment command consumes V41R2 regardless of outcome. A second
scientific attempt is not authorized. The execution record must retain job,
attempt, restart, spend, artifact, and validation facts. The unchanged inner
artifact claim is `RemoteH100RuntimeProfileOnlyV41`; V41R2 labels only the
orchestration recovery envelope. No acquisition or continual-learning claim
follows from runtime-profile success.
