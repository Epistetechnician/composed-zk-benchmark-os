# Astral Gemma 3 V4 terminal freeze

State slice: `astral-trace-completeness-gemma3-end-to-end-v4`.

V4 is permanently frozen as qualification-only historical evidence. The
sealed qualification digest is
`9c90364b29e6992539323acea147ba0e9bd746578f40b69f0d9f3f1438493eab`. Its
pooled global-centered NMSE was `0.04572051752036069` against the unchanged
`<=0.05` gate. Native parity, deterministic repeatability, no-op identity,
zero-intervention reach, exact event replay, and feature repeat stability
passed. Independent reconciliation passed and the raw root was emptied.

This freeze prohibits V4 source, corpus, activation, effect, prediction,
assessment, and result mutation. V4 does not establish semantic feature
validity, a causal graph, held-out causal scrubbing, kernel-complete
observability, introspection, causal self-modeling, Stage 0C, Stage 1,
benchmark evidence, or production readiness. The V1 causal-feature-effects
slice may reuse only the exact model/runtime/asset identity as a separately
custodied dependency; V4 scientific result bytes are excluded inputs.

The V4 qualification ceiling remains
`LocalDevelopmentGemma3EndToEndCausalTraceQualificationV4`. No V4 assessment
receipt exists and no V4 assessment effect is authorized.
