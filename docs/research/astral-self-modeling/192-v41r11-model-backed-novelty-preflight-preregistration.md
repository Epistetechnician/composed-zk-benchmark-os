# V41R11 Model-Backed Novelty Preflight Preregistration

State slice: `V41R11ModelBackedNoveltyPreflightAuthorizationAndExecution`.

Status: `IndependentValidatorImplemented / ExecutionUnauthorized`.

The next bounded experiment is restricted to two full V41R11 panels on the
pinned unchanged GPT-OSS-20B checkpoint: context-free and context-only. It must
retain all raw four-candidate normalized likelihoods, exact decisions, model and
runtime identity, source hashes, geometry, native-MXFP4 configuration, memory,
and manifest integrity.

The independent validator reconstructs the V41R11 instrument, recomputes all
96-query metrics per panel, checks every normalized decision, reapplies the
frozen novelty and positive-control gates, verifies zero adapter/update/optimizer
activity, confirms tune and assessment are absent, and binds every source file
to the committed RGS tree.

No GPU or model execution is authorized by this document. A later immutable
context and exact one-shot authorization are required. The first terminal
outcome consumes the identity. A pass reaches only
`RemoteH100ModelBackedNoveltyPreflightV41R11`; training and all scientific
promotion beyond model-backed novelty remain unauthorized.
