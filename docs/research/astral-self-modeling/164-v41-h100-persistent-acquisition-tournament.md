# V41 H100 Persistent-Acquisition Tournament

State slice: `V41H100PersistentAcquisitionTournament`.

Status: `ProspectivelyLocked / RuntimeProfileNotRun`.

The highest-information use of the available external H100 budget is a
GPT-OSS-20B persistent-acquisition tournament, not an Astral telemetry run.
The unresolved prerequisite is whether fresh opaque relational knowledge can
be acquired into weights, retained across later tasks, and protected without
source context.

The RGS contract freezes one H100 batch execution, model revision
`d0e2aa76789354d715f8b22553b9feb6c462fcf0`, Harmony formatting, official
MXFP4-dequantized LoRA geometry, three update policies, two nonpersistent
controls, three seeds, three task orders, four fresh tasks, direct/paraphrase/
composition evaluation, restart and context removal, protected capability
checks, calibration, reload parity, and exact recovery.

Spend is sequentially gated at `$15`, `$45`, `$240`, and `$100`, with `$50`
sealed and a total ceiling of `$450`. Astral remains blocked unless at least
two persistent arms qualify and create a measurable tradeoff.

Paid execution has not started. The configured GiveMeNode MCP server did not
expose callable tools to this task, so billing and job submission could not be
verified or performed. No alternate transport or credential bypass is
permitted.

The maximum eventual claim is model-, corpus-, implementation-, and
qualification-scoped persistent acquisition and retention. This is not
autonomous self-improvement, introspection, Stage 0C, independent replication,
or a breakthrough.
