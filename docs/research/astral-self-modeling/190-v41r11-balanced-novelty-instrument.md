# V41R11 Balanced Novelty Instrument

State slice: `V41R11NoveltyInstrumentDesignAndLocalQualification`.

Status: `IndependentLocalValidatorImplemented / ModelExecutionUnauthorized`.

V41R10 stopped before training because its no-update result exceeded the
frozen novelty ceiling. The retained rows show that its varying lexical target
panels did not neutralize checkpoint preferences. V41R11 makes one bounded
correction: all 32 cases share four opaque labels, while targets, label
positions, and target positions are exactly balanced.

The independent Astral implementation reconstructs all subject and bridge
identities from the fixed version and seed commitment. It verifies the complete
packet hash, 32-case census, 96-query census, eight targets per label, eight
appearances of every label in every candidate position, eight targets in every
position, withheld paraphrase/composition prompt disjointness from training,
and absence of source, tune, and assessment material. Direct queries remain the
explicit trained-form acquisition panel. The validator does not import the RGS
generator.

Passing both local validators establishes only
`LocalQualifiedNoveltyInstrumentV41R11`. Model-backed novelty still requires a
separately authorized unchanged-checkpoint run. No training, adapter, tune,
assessment, acquisition, continual-learning, or self-improvement conclusion is
authorized by this slice.
