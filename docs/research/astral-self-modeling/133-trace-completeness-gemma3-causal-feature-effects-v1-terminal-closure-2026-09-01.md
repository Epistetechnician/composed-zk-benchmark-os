# Astral Gemma 3 Causal Feature-Effects V1 Terminal Closure

State-slice mutation: `astral-trace-completeness-gemma3-causal-feature-effects-v2`.

V1 is closed permanently as `NoCandidate` under its own immutable state slice
`astral-trace-completeness-gemma3-causal-feature-effects-v1`. The corrected V1
execution passed its qualification, custody, parity, repeatability, control,
power, tune, and independent aggregate-validation gates, but the locked fit
feature-effect gate failed. V1 therefore produced no held-out causal claim.

The V1 packet, corpus, activation traces, feature effects, prediction lock,
raw events, aggregate result, provider allocation, and review receipts remain
historical V1 evidence only. They are not copied, loaded, joined, or used as
scientific inputs by V2. No V1 threshold, asset, feature, prompt, result, or
claim may be retuned or reopened.

V1 claim ceiling: `LocalDevelopmentGemma3CausalFeatureEffectsQualificationV1`.

V2 is a fresh protocol identity and must independently satisfy its own packet,
custody, review, qualification, fit, and held-out scrubbing gates.
