# V28R7 Single-Cell Acquisition Pilot Preregistration

State slice:
`astral-rgs-v28r7-single-cell-acquisition-pilot-preregistration`.

Status: `DocsFirstPreregistered / ImplementationNotAuthorized / NotRun`.

V28R7 screens the seven frozen persistent mechanisms before committing to the
historical 63-cell Gate 1 matrix. It is a development pilot, not a reduced
qualification experiment.

After an exclusive ledger and seed commitment, generate one fresh 6,144-family
corpus that has no exact, normalized, structural, semantic, or template reuse
through V28R6. Rank all 64 complete blocks by a seed-bound SHA-256 selector and
lock the lowest eight blocks as a balanced 768-family, 9,216-query evaluation
panel. Train from all 6,144 source-only rows. Permanently exclude the corpus
from qualification and assessment.

At frozen batch 8, run two fresh unchanged-checkpoint novelty baselines,
context and retrieval controls, then all seven persistent arms at seed `280301`
and `order-abcd`. Each arm receives the full Gate 1 budget: rank 8, six layers,
768 steps, eight examples per step, 128 tokens per example, 786,432 update
tokens, learning rate `1e-4`, and at most 64 MiB state. Preparation, update, and
evaluation must be separate processes; failures are consuming and retained.

Novelty failure stops before update. A method is a pilot signal only if its
overall and dimension accuracies and conservative cluster bounds pass the
frozen `0.70`/`0.60` floors, mean paired family gain is at least `0.20`, the
10,000-draw fact-kind-stratified Bonferroni lower bound is above `0.20`, RSS is
at most 8 GiB, and every provenance/budget/artifact gate passes.

At least two signals yield `PilotSignalPresent`; one yields
`PilotSingleSignal`; none yields `PilotNoSignal`. The maximum claim is
`LocalSingleSeedOrderAcquisitionPilotV28R7`. A positive pilot authorizes only a
fresh powered preregistration, never continual-learning, self-improvement,
SOTA, confirmation, or independent-replication claims.
