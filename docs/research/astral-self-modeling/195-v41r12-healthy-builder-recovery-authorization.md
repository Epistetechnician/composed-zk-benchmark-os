# V41R12 Healthy-Builder Recovery Authorization

State slice: `V41R12HealthyBuilderRecoveryExecution`.

Status: `ConsumedByNoveltyPreflightPassed / TrainingUnauthorized`.

Provider ticket `tkt-ubg9g` establishes that R1 failed before context fetch on
a low-disk builder, the worker was drained, five full pipeline canaries passed,
no cache was poisoned, no GPU attempt began, and the user was not charged.

V41R12 freezes a distinct execution identity while keeping RGS
`a9cd29c2e6aa19d8369f0edbce63380e4673e4cb`, Astral
`4a9290c750fdae164f3cd114442a96feb735c781`, context `ctx-35bb0f03`, every
context and source hash, checkpoint, runtime, command, query, and gate
byte-identical. Exactly 96 no-update and 96 context-only decisions are allowed.
Adapter construction, optimization, updates, tune, and assessment remain
forbidden.

One clock-locked H100, zero restarts, 120 minutes, and USD 5.40 are authorized.
The first terminal outcome consumes V41R12. A pass may establish only
`RemoteH100ModelBackedNoveltyPreflightV41R11` under the V41R12 recovery
identity and open a separately reviewed
acquisition-pilot design.

Job `job-3nn6y` consumed V41R12 with a valid pass. See
`196-v41r12-novelty-preflight-execution-record.md`.
