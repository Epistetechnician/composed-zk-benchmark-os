# V34 Protected Replay Implementation

Status: `Implemented / HermeticValidationComplete / ModelExecutionUnauthorized`.

The one-process worker runs the fixed 25% and 50% replay mixtures and retains
mixture counts, loss/gradient traces, adapters, likelihood decisions, and V30
protection evidence. Astral independently reconstructs its fixture, gates,
selection, source locks, and artifact census.
