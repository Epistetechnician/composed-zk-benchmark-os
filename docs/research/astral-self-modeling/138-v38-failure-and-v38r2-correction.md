# V38 Failure and V38R2 Correction

Status: `V38ConstructionFailed / V38R2CorrectionAuthorized`.

V38 is consumed. It completed only stage 1 of the first cell before the worker
called nonexistent `_max_delta` instead of the implemented `max_delta` during
reload comparison. The retained directory is
`incomplete-astral-rgs-v38-0c63d3cb`. No model result or scientific
classification exists.

V38R2 may change only that call site and every experiment identity to V38R2.
All arms, schedules, prompts, seeds, orders, budgets, gates, and claim ceilings
remain frozen. Execution requires a new exact-commit authorization.
