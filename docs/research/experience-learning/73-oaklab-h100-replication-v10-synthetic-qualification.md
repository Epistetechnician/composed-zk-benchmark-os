# Oak Lab H100 replication V10 synthetic qualification

State slice: `oaklab-experience-learning-h100-replication-v10`.

The independently accepted V10 packet was qualified on the declared fit-only
synthetic cohort: 48 seeds, six streams, 256 rows per trajectory, eight
32-row segments, and the locked learning/controller constants. The runner
materialized 288 family/seed rows for fixed batch-one SGD and the
dual-budgeted segment-credit treatment. Metrics, resource counters, and the
qualification gate were derived from those rows. Tune and assessment cohorts
remain absent.

Disposition: `no_candidate`. The strict gate did not pass. This is a synthetic
qualification result only and does not authorize real data, provider spend,
H100 execution, energy capture, assessment, or publication. Real execution
requires a separate authorization, fresh custody and campaign manifests, and
a privileged workload-specific energy receipt. V9 and all earlier Oak Lab
identities remain closed; Astral remains isolated.

Result artifact: `experiments/experience_learning/oaklab_h100_v10_synthetic_qualification.json`.

Every mutation in this phase names state slice
`oaklab-experience-learning-h100-replication-v10`.
