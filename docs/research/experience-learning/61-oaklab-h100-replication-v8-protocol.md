# Oak Lab H100 replication V8 protocol

State slice: `oaklab-experience-learning-h100-replication-v8`.

V8 is a new unused protocol identity after V7's terminal rejection. V7, V6,
Phase 836, and the plasticity guard are historical and cannot be scientific
inputs. Astral remains isolated. This phase is contract/compiler/review only
until an independent packet-bound signed `ACCEPT` exists.

## Corrected campaign binding

The canonical source is
`experiments/experience_learning/oaklab_h100_v8_protocol.json`. It declares a
materialized campaign-manifest artifact at
`experiments/experience_learning/oaklab_h100_v8_campaign_manifest.json`.
The artifact is independently validated against the current source, compiled
file, review packet, compiler, validator, tests, `AGENTS.md`, and fixed
backend/guard/model/data declarations. It is a pre-execution declaration only;
execution additionally requires a separate actual campaign manifest path whose
contents validate completely before authorization.

Execution authorization must rerun full validation, load that actual manifest
from the plan's path, require its self-digest and core digest, and match its
source, compiler, validator, tests, `AGENTS.md`, compiled artifact, review
packet, campaign, custody, provider, lock, cost, stop, result-root, and energy
bindings. A caller-supplied manifest digest without a validated file is
invalid.

## Closed contract and gates

The compiler and validator recursively enforce the complete estimand,
controller, generator, recurrence, numeric AST, byte layout, statistics,
adaptation, lock, counter, provider, energy, result-root, and assessment-absence
schemas. Family statistics, Holm adjustments, event denominators, and resource
metrics are derived from raw rows and counters. Prediction locking must precede
assessment. Provider allocation, node, USD ceiling, cost, timestamps, and stop
receipts are cross-bound. Phase 836, V6, plasticity guard, and Astral remain
explicitly isolated.

No learner, model, dataset, provider, GiveMeANode allocation, H100 job, spend,
energy capture, assessment, SOTA claim, or publication is authorized before a
new independent packet-bound signed `ACCEPT`. After `ACCEPT`, synthetic
qualification remains the next gate; real execution requires a separate
authorization, fresh custody, actual manifest, privileged joules, and the
strict multi-family publication gate.

Every mutation in this phase names state slice
`oaklab-experience-learning-h100-replication-v8`.
