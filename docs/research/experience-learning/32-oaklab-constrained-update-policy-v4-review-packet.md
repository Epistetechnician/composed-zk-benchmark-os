# Oak Lab constrained update policy V4 review packet

State slice: `oaklab-experience-learning-constrained-update-policy-v4`

Review status: `pending`

Implementation authorization: `false`

Real-execution authorization: `false`

## Required decision

The independent reviewer must return exactly `accepted` or `rejected` for the
frozen protocol identity. Any blocking ambiguity is `rejected`. Acceptance may
authorize additive synthetic implementation only. It cannot authorize fit,
tune, assessment, real panels, energy capture, publication, the plasticity
guard, or Astral.

## Review scope

The reviewer must independently check:

1. the policy-level estimand and paired identification assumptions;
2. action timing, terminal handling, complete carryover, and pre-update loss;
3. true-online Sarsa recurrence, feature layout, reward timing, trace resets,
   dual update, exploration assignment, and nonfinite handling;
4. exact PRNG and every stream equation, seed cohort, source ID, target, event,
   oracle feature, shift point, and experimental unit;
5. fit-only mutation, zero model resets, tune eligibility, canonical lock,
   and prohibition on assessment materialization before lock acceptance;
6. per-seed endpoints, family normalization, paired statistic, Holm rules,
   zero-variance handling, power scope, and full gate order;
7. adaptation-lag baseline, windows, censoring, aggregation, margin, and strict
   improvement requirement;
8. operation and byte algebra, candidate/reference symmetry, fit-versus-
   assessment accounting, replay prohibition, and measured-energy separation;
9. noise/oracle/null controls, lambda-zero, no-dual, matched-random,
   reward-shift, always-skip, and twin-oracle falsification gates;
10. validator recomputation and mandatory rejection behavior;
11. permanent guard closure, V1-V3 closure, V2-energy ineligibility, claim
    ceiling, real-execution seal, and Astral isolation.

## Fail-closed implementation conditions

If accepted, implementation must be V4-only and additive. It must not alter
the frozen protocol, V1-V3, the guard, shared stream semantics, or prior
receipts. Before any fit effect, a second independent check must bind source
digests and independently validate generator, action, model-update,
controller, counter, snapshot, and tamper oracles. Any mismatch closes V4.

The review receipt must bind exact SHA-256 identities for `AGENTS.md`, the
machine protocol, this human protocol, this packet, V3 terminal closure, and
the permanent guard closure. The receipt itself must be canonical JSON with a
recomputed digest and an explicit claim ceiling.
