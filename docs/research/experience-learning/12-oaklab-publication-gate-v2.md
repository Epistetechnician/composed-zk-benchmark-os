# Oak Lab full-campaign publication gate V2

State slice: `oaklab-experience-learning-benchmark-v2`

The gate combines the powered guard receipts, all-baseline real matrix,
real-derived nonstationary matrix, and longer event matrix. It requires one
mechanism to beat fixed SGD on mean loss and adaptation lag while being no
worse on updates, active operations, state, and replay storage across at
least two stream families, with paired statistics and powered cohorts. A
measured hardware energy receipt is mandatory.

Receipt:

`/Users/shaanp/Documents/research-artifacts/oaklab-experience-learning-publication-gate-v2-r4/publication_gate.json`

Gate digest: `4994b0a5c964005a70553fdf35dda97104e4ae4832b2042aebb47bc09ce4f1f7`.

Independent validation passes. Status is `no_candidate`:

- powered assessments: pass;
- all-baseline and control coverage: pass;
- quality/adaptation/resource frontier: no qualifying algorithm;
- measured hardware energy: missing because privileged `powermetrics` was
  unavailable (`sudo -n` required a password).

The claim ceiling remains
`LocalDevelopmentOakLabExperienceLearningBenchmarkV2`. The next mechanism
must be materially new; rerunning the failed guard with retuned thresholds is
not authorized by this result.
