[package]
name = "hsai-claim-envelope"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
description = "Pure claim-envelope algebra for Hyper Sacred AI."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
proptest = { version = "=1.5.0", default-features = false, features = ["std"] }
